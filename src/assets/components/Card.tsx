import React from "react";

type Props = {
  productList: Product[];
};

type Product = {
  id: number;
  name: string;
  organic: boolean;
  food: boolean;
  price: number;
  unit: string;
  image: string;
};

const Card = () => {
  return null;
};

export default Card;
